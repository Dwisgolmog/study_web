1.Uniform interface(가장 중요!!)
-하나의 자료는 하나의 URL로
-URL 하나를 알면 둘을 알 수 있어야함
-요청과 응답은 정보가 충분히 들어있어야함

2.Client-Server 역할 구분
-브라우저는 요청만 해야한다.
-서버는 응답만 해야한다.

3.Stateless
-요청1과 요청2는 의존성이 없어야함

4.Cacheable
-서버에서 보내주는 정보들은 캐싱이 가능해야한다.

5.Layered System

6.Code on Demand

==============================
이름짓기 원칙:
URL을 명사로 작성하는것이 좋음
하위 문서를 나타낼땐 / 로 구분하고 파일 확장자(.html)쓰지 말것
띄어쓰기는 대시(-)를 이용할 것
자료 하나당 하나의 URL을 사용(하위 폴더 만들듯이 세분화하여 만들것)
ex) instagra.com/explore/tags/kpop/